# Cubex Compiler Test 1 - Stage 1
ret := [];


for ( i in input)
{
	# atoi
	n := 0;
	for (c in i) {
		n := n * 10;
		n := n + c.unicode() - 48;
	}

	n := n + 1;

	#itoa
	m := [];

	while(n > 0) {
		for ( k in n % 10) {
			m := [character(k + 48)] ++ m;
		}
		for ( k in n / 10) {
			n := k;
		}
	}

	ret := ret ++ [string(m)];
}
return ret;