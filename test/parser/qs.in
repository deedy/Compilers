fun quicksort(l : List) : List
{
	m := l.first();
	n := l.pop();
	a := newList();
	b := newList();
	for(i in 1..(l.range())){
		if(l.get(i) > m){
			j := a.put(l.get(i));
		}else{
			k := (b.put(l.get(i)));
		}
	}
	newList := a.append(m,b);

	# will not terminate trololololo
	return quicksort(newList);
}
return 0;