class Rev () {
    fun rev (str : String) : String {
        if ((null == str) | (str.length()  <= 1)) {
            return str;
        }
        return rev(str.substring(1)) + str.charAt(0);
    }
}

return 1;