count := 0;
for(i in 0.<n){
	for(j in i<<n){
		mondad := printer.print("This is an n squared function");
		# comments
		for( k in (j + 1)..(n - 1)){
			# comments
			# on comments
			# return "i put code in comments!"
			s := "we need to go deeper";
			for(l in (k + 1).<n) ` we went too deep' return "we went too deep";
		}
	}
}