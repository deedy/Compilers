class Fib () {
    super();

    fun fib(n : Integer<>) : Integer<> {
        if ( n == 1 | n == 2 ) {
         return 1; 
        }
        first := 1;
        second := 1;
        for(el in [1,2,3]) {
            tmp := second;
            second := first + second;
            first := tmp;
            n := n - 1;
        }
        return second;
    }
}

return ["sup"];